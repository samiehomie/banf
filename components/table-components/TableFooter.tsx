import React from 'react'

function TableFooter({ children }: { children: React.ReactNode }) {
  return <section className="mt-[45px] ml-[-35px] flex">{children}</section>
}

export default TableFooter
